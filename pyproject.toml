[project]
name = "musteribulma"
version = "0.1.0"
description = "Türkiye B2B lead generation — Apify + Flask"
requires-python = ">=3.12,<3.13"
dependencies = [
    "apify-client>=1.7.0",
    "flask>=3.0.0",
    "python-dotenv>=1.0.0",
    "requests>=2.31.0",
]

[tool.vercel]
entrypoint = "api.scrape:app"
